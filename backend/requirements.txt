# ── Web Framework ──────────────────────────────
Django==5.0.7
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-filter==24.3
drf-spectacular==0.27.2

# ── Task Queue ─────────────────────────────────
celery==5.4.0
redis==5.0.8
django-celery-beat==2.6.0

# ── Web Scraping / Crawling ────────────────────
feedparser==6.0.11
requests==2.32.3
beautifulsoup4==4.12.3

# ── Utils ──────────────────────────────────────
python-dotenv==1.0.1
Pillow==10.4.0

# ── ML (lightweight, без torch) ────────────────
# Для работы без GPU и тяжёлых моделей используется
# keyword-based fallback в ml/analyzer.py.
# Раскомментируй ниже если хочешь BERT-модели:
#
# torch==2.4.0+cpu --index-url https://download.pytorch.org/whl/cpu
# transformers==4.44.2
# scikit-learn==1.5.1
# numpy==1.26.4
